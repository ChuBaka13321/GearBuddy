<h1>Welcome to GearBuddy!</h1>
<ul>
  <li><h3>Share and Upload Your Loadout!</h3></li> 
  <li><h3>Tell the community what works for you!</h3></li>
  <li><h3>Help new enthusiasts get an idea of how to setup their gear, as well as learn from others!</h3></li>
</ul>
    <%= link_to "Start here and share a loadout!", new_loadout_path(@loadout) %><br><br>
        
        <% @loadouts.each do |l| %>

          <strong> <%= l.name %> </strong> <br>
            <div class="user_box">
              
                  <% if l.user.photos %>
                    <% l.user.photos.each do |p| %>
                      <%= image_tag p.image.url(:medium) %><br/>
                    <% end %>
                  <% end %>
                </div>
              
            <br>

          <% if l.user == current_user %>
            <strong><%= link_to "This is your loadout, add a new image!", new_photo_path(@photo) %></strong><br>
          <% end %>
          <%= link_to "More details!", loadout_path(l) %>
          <br><br><br>
      <% end %>

